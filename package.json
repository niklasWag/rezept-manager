{
  "name": "rezept-manager",
  "version": "0.0.1",
  "scripts": {
    "setup": "node scripts/setup.js",
    "start": "node-run-all npm run st",
    "start-backend": "cd ./backend && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "listr": "^0.14.3",
    "npm-run-all": "^4.1.5"
  }
}
