{
  "name": "rezept-manager",
  "version": "0.0.1",
  "scripts": {
    "docker": "docker compose -p rezept-manager up -d",
    "setup": "npm i && node scripts/setup.js",
    "start": "npm run setup && npm run docker && npm-run-all --parallel start-backend start-frontend",
    "start-backend": "cd ./backend && npm start",
    "start-frontend": "",
    "test": "npm run docker && cd backend && npm test"
  },
  "devDependencies": {
    "listr": "^0.14.3",
    "npm-run-all": "^4.1.5"
  }
}
